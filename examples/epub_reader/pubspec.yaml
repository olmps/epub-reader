name: epub_reader
description: Example for the epub_renderer package
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  epub_renderer:
    path: ../../packages/epub_renderer

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/book.epub
